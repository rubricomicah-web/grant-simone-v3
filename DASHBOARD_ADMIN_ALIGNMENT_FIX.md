# Dashboard and Admin Alignment Fix

This build applies targeted CSS overrides for the actual dashboard/admin selectors.

Fixes:
- Dashboard feature cards no longer overflow into each other.
- Admin plan cards are restored to a wide landscape 3-column layout.
- Text no longer stacks vertically in admin cards.
- Plan cards use proper min widths on desktop and stack only on smaller screens.
- Previous over-broad admin-plans class usage was cleaned from dashboard grids.
